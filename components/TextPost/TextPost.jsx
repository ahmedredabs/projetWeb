import {Text} from 'react-native';
import React from "react";
const TextPost = (props) => {
    return (
        <div>
            <Text>{props.textPost}</Text>
        </div>
        
    )
}

export default TextPost;